% Paper-ready Results/Evaluation section (IEEE style).
% Drop this into your main IEEEtran document and adjust figure numbering as needed.
%
% Recommended packages in main preamble:
% \usepackage{graphicx}
% \usepackage{booktabs}
% \usepackage{multirow}
%
% Figure paths are set in main.tex via \graphicspath{{figures/}}.

\section{Results and Evaluation}
\label{sec:results}
The following evaluation validates that the proposed ISA-visible authorization instruction, constant-time comparator, and security FSM successfully enforce on-device execution and debug gating, demonstrating the effectiveness of the key contributions outlined in Section~\ref{sec:introduction}.

\subsection{Experimental Workflow}
Evaluation was conducted at two levels: (i) \emph{toolchain-level validation} confirming that the custom authorization instruction is supported end-to-end (compile, assemble, link, and produce hex images), and (ii) \emph{system-level validation} confirming that both execution and debug access are gated by authorization. Evidence was collected from host-side application logs and screenshots extracted from the development artifacts.

\subsection{Evidence Artifacts}
\textbf{Toolchain validation.} Fig.~\ref{fig:compile} shows compilation of a test program that invokes the custom instruction via inline assembly, along with successful ELF-to-HEX generation reported by the tool. This demonstrates that the custom instruction can be emitted from C code and preserved across the build pipeline without manual binary patching.

\textbf{Firmware inspection (HEX viewer).} To support operator verification and troubleshooting, the host application provides a hex viewer for generated images. Fig.~\ref{fig:hex} shows an example view of the generated hex contents.

\begin{figure*}[!b]
  \centering
  \begin{minipage}{0.49\textwidth}
    \centering
    \includegraphics[width=\linewidth]{fig_compile.png}
  \end{minipage}\hfill
  \begin{minipage}{0.49\textwidth}
    \centering
    \includegraphics[width=\linewidth]{fig_hex.png}
  \end{minipage}
  \caption{Toolchain and firmware inspection evidence: (left) compilation and ELF$\rightarrow$HEX generation with the custom instruction; (right) hex viewer output for the compiled image.}
  \label{fig:compile}
  \label{fig:hex}
\end{figure*}

\textbf{Authorization and debug enablement.} The core functional requirement is that debug access is released only after successful authorization. Fig.~\ref{fig:auth} summarizes the authorization flow evidence: (top) key exchange and matching indication in the application log, and (bottom) the resulting debugger enablement after a successful match.

\begin{figure*}[!b]
  \centering
  \begin{minipage}{0.49\textwidth}
    \centering
    \includegraphics[width=\linewidth]{fig_keymatch.png}
  \end{minipage}\hfill
  \begin{minipage}{0.49\textwidth}
    \centering
    \includegraphics[width=\linewidth]{fig_debugenabled.png}
  \end{minipage}
  \caption{Authorization and debug gating evidence: (left) key exchange and successful match indication; (right) debugger enabled after a successful match.}
  \label{fig:auth}
\end{figure*}

\textbf{Connection and programming workflow.} Fig.~\ref{fig:program} summarizes the programming workflow evidence: (top) a successful connection state, and (bottom) a programming session log including the \texttt{load\_image} operation for a firmware ELF.

\begin{figure*}[!b]
  \centering
  \begin{minipage}{0.49\textwidth}
    \centering
    \includegraphics[width=\linewidth]{fig_connected.png}
  \end{minipage}\hfill
  \begin{minipage}{0.49\textwidth}
    \centering
    \includegraphics[width=\linewidth]{fig_loadimage.png}
  \end{minipage}
  \caption{Programming workflow evidence: (left) host tool indicates successful device connection; (right) host log includes \texttt{load\_image} for the compiled firmware ELF.}
  \label{fig:program}
\end{figure*}

\textbf{Summary.} The collected artifacts confirm that the custom instruction compiles
and is preserved through ELF$\rightarrow$HEX generation, hex inspection is supported,
authorization precedes debug enablement, and the host workflow supports connection and
programming with explicit image loading.

% Allow text to flow to reduce blank space.

